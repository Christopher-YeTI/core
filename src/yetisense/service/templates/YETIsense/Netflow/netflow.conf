#
# Automatic generated configuration for netflow.
# Do not edit this file manually.
#
{% if not helpers.empty('YETIsense.Netflow.capture.interfaces') and not helpers.empty('YETIsense.Netflow.capture.targets') %}
netflow_interfaces="{% for interface in YETIsense.Netflow.capture.interfaces.split(',')
%}{{
    helpers.physical_interface(interface)
}} {%
  endfor%}"
netflow_egress_only="{% if YETIsense.Netflow.capture.egress_only %}{% for interface in YETIsense.Netflow.capture.egress_only.split(',')
%}{{
    helpers.physical_interface(interface)
}} {%
  endfor%} {%endif%}"
netflow_version="{%if YETIsense.Netflow.capture.version == 'v9' %}9{% else %}5{%endif%}"
netflow_int_destination="127.0.0.1:2055"
netflow_destinations="{% for item in YETIsense.Netflow.capture.targets.split(',') -%}
{% if item.count(']') %} {{ item|replace(']:', ']/') }} {% else %} {{ item|replace(':','/')}} {% endif -%}
{% endfor %}"
netflow_active_timeout={{YETIsense.Netflow.activeTimeout|default(1800)}}
netflow_inactive_timeout={{YETIsense.Netflow.inactiveTimeout|default(15)}}
{% endif %}
